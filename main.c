#include "monty.h"

/**
 *
*/
int main(int ac, char **av)
{
	/*int choice;
	

	while (choice != 3)
	{
		printf("Enter choice: ");
		scanf("%d", &choice);

		switch (choice)
		{
			case 1:
				push();
				break;
			case 2:
				pall();
				break;
			default:
				exit(0);
		}
	}*/
	file_handle(ac, av);
	return (0);
}
